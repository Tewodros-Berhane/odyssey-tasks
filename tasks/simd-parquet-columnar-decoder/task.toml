[metadata]
name = "simd-parquet-columnar-decoder"
title = "Zero-Copy SIMD Columnar Parquet Decoder with Predicate Pushdown"
slug = "simd-parquet-columnar-decoder"
collection_family = "Library clone"
task_family = "performance"
verifier_family = "programmatic"

[environment]
cpus = 8
memory_mb = 16384
storage_mb = 10240
gpus = 0
network_mode = "none"

[agent]
timeout_sec = 28800
network_mode = "none"

[verifier]
timeout_sec = 1800
network_mode = "none"
