[metadata]
name = "Idempotent Multi-Tenant Escrow & Double-Entry Ledger API with HMAC Reconciliation"
slug = "idempotent-ledger-escrow-api"
collection_family = "Product clone"
task_family = "feature_development"
verifier_family = "programmatic"

[environment]
cpus = 4
memory_mb = 8192
storage_mb = 4096
gpus = 0
network_mode = "none"

[agent]
timeout_sec = 28800
network_mode = "none"

[verifier]
timeout_sec = 1800
network_mode = "none"
